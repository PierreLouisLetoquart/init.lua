# Init.lua

Personal nvim config.

```
brew install ripgrep fzf

# C++
brew install clang-format

# CSS, HTML, JS, JSX, JSON, Markdown
npm install -g prettier

# Lua
brew install stylua

# Rust
rustup component add rustfmt

# TypeScript / TSX
npm install -g @biomejs/biome

# Spell checker
brew install codespell
```
